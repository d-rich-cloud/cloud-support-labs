# AWS CLI Commands – Basics

## Setup

- Check AWS CLI version
  ```bash
  aws --version
    aws configure
    aws s3 ls
    aws s3 cp myfile.txt s3://my-bucket/
    aws ec2 describe-instances --region us-east-1
  
